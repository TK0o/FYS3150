compiled and linked by g++ filename.cpp -o ../exe/filename.exe -larmadillo

add -fopenmp to use parallelization

and executed by ../exe/filename.exe

python files are run as usual and does not need any input.
